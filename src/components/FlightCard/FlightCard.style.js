const { StyleSheet } = require("react-native");

const styles = StyleSheet.create({
    container: {
      
      width:"50%",
      padding: 24,
     
      borderWidth:2,
      borderColor:"blue",
      
    },
    text: {
      fontSize: 30,
      color: '#000'
    },
  });
  export default styles;